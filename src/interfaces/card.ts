export interface Card {
  id: string;
  title: string;
  content: string;
  sectionId: string;
  createdAt: string;
}

export interface NewCard {
  id: string;
  title: string;
  content: string;
  sectionId: string;
}
